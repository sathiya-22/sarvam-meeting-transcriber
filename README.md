# 🎤 Sarvam Meeting Transcriber
Timestamped transcription of Indian language meetings + AI summary.

```bash
pip install -r requirements.txt && cp .env.example .env
python transcriber.py meeting.mp3 hi-IN transcript.txt
python transcriber.py call.wav ta-IN output.txt
```
